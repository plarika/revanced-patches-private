name: Pull strings (private fork)

on:
  workflow_dispatch:

jobs:
  disabled-until-configured:
    name: Crowdin not configured
    runs-on: ubuntu-latest
    steps:
      - name: Explain private-fork state
        run: |
          echo "Crowdin synchronization is intentionally disabled in the private FantaMK fork."
          echo "Configure private Crowdin credentials and review the upstream workflow before enabling it."
